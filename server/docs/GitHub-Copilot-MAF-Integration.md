# GitHub Copilot + MAF ??????

## ????
2026-05-22

## ????

????? GitHub Copilot ?????? Process ???????????????? Microsoft Agent Framework (MAF) ??????????

## ????

### 1. ?????
- **`IGitHubCopilotService`**: ?? GitHub Copilot ???????
- **`GitHubCopilotCliService`**: ??????? CLI ????

### 2. ?? `AgentFrameworkGateway`
- ???????? `IGitHubCopilotService`
- ????? Process ????
- ????????

### 3. ????
```
src/AutoCodeForge.Infrastructure/AI/
??? AgentFrameworkGateway.cs          (??)
??? IGitHubCopilotService.cs          (??)
??? GitHubCopilotCliService.cs        (??)
```

## ???? (v1 - CLI Based)

### GitHubCopilotCliService
- ???? GitHub Copilot CLI ????
- ????????
- ???? (60 ?)
- ?????? (PAT, Organization)
- ???????

### ????
1. **????**: ?? `SemaphoreSlim` ?????????? CLI ??
2. **????**: ???????
3. **????**: ??????????
4. **?????**: ????? CLI ?????????

## ?????? (v2 - SDK Based)

### ????
? GitHub.Copilot.SDK ???(????:1.0.0-beta.6)????????:

```csharp
public class GitHubCopilotSdkService : IGitHubCopilotService
{
    private readonly CopilotClient _copilotClient;
    private readonly AIAgent _copilotAgent;
    
    public async Task InitializeAsync(LLMModelConfigEntity model)
    {
        var options = new CopilotClientOptions
        {
            AutoStart = true,
            LogLevel = "info",
            CliPath = model.CliExecutable
        };

        _copilotClient = new CopilotClient(options);
        await _copilotClient.StartAsync();

        var sessionConfig = new SessionConfig
        {
            Model = model.ModelName ?? "gpt-5",
            Streaming = false,
            OnPermissionRequest = (_, _) => Task.FromResult(
                new PermissionRequestResult 
                { 
                    Kind = PermissionRequestResultKind.Approved 
                })
        };

        // ??????AsAIAgent ?? Copilot ? MAF
        _copilotAgent = _copilotClient.AsAIAgent(
            sessionConfig: sessionConfig,
            ownsClient: false,
            id = "copilot-agent",
            name: model.ModelName ?? "GitHub Copilot",
            description: "MAF agent backed by GitHub Copilot"
        );
    }

    public async Task<LlmResponse> ExecuteAsync(...)
    {
        var result = await _copilotAgent.RunAsync(prompt, cancellationToken);
        return new LlmResponse { Content = result.Text, ... };
    }
}
```

### SDK ?????
1. **?? MAF ??**: `CopilotClient.AsAIAgent()` ???? `AIAgent`
2. **????**: ?? `RunStreamingAsync()`
3. **????**: ???? MAF ????
4. **????**: ????????????
5. **????**: ??????????

### ????
1. ?? GitHub.Copilot.SDK ??????
2. ?? `GitHubCopilotSdkService` ?? `IGitHubCopilotService`
3. ?????????????? CLI ? SDK ??
4. ???? `AgentFrameworkGateway` ????????

## ????

### ?? CLI ??
```json
{
  "modelName": "gpt-5",
  "provider": "GitHubCopilot",
  "cliExecutable": "copilot",
  "organization": "my-org",
  "patEnvVar": "GITHUB_TOKEN",
  "apiKey": "ghp_xxxxx"
}
```

### ?? SDK ?? (????)
????????????????

## ????

### Program.cs ????
```csharp
// ?? CLI ??
services.AddScoped<IGitHubCopilotService, GitHubCopilotCliService>();

// ?? SDK ??
// services.AddScoped<IGitHubCopilotService, GitHubCopilotSdkService>();
```

## ????

### ????
- `AgentFrameworkGatewayTests`: ??????
- GitHub Copilot ???????? (?? CLI ??)
- ????? `[Skip]` ????????

### ??????
1. ?? CLI ????
2. ??????
3. ??????
4. ????????

## ????
- MAF + Copilot CLI Integration Guide (???? V1.0)
- GitHub.Copilot.SDK (??: beta.6, ??: 1.0.0 stable)
- Microsoft.Agents.AI (1.0.0-preview.251016.1)

## ??

??????:
1. ? ????????????
2. ? ? SDK ????????
3. ? ??????
4. ? ???????????
5. ? ?? SOLID ?? (????)

???????? `IGitHubCopilotService` ??????? SDK ????? MAF ??????
